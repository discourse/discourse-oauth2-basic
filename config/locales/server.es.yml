# encoding: utf-8
#
# Never edit this file. It will be overwritten when translations are pulled from Transifex.
#
# To work with us on translations, join this project:
# https://www.transifex.com/projects/p/discourse-org/

es:
  site_settings:
    oauth2_enabled: "Habilitar OAuth2 personalizado"
    oauth2_client_id: 'ID de cliente del OAuth2'
    oauth2_client_secret: 'Secreto de cliente del OAuth2'
    oauth2_authorize_url: 'URL de autorización del OAuth2'
    oauth2_token_url: 'Token de URL del OAuth2'
    oauth2_user_json_url: 'URL de la que obtener el JSON del usuario para OAuth2 (:id se sustituirá con la id devuelta por OAuth y :token con la id del token)'
    oauth2_json_user_id_path: 'Ruta en el JSON de usuario de OAuth2 para obtener la id del usuario. Ej.: user.id'
    oauth2_json_username_path: 'Ruta en el JSON de usuario de OAuth2 para obtener el nombre de usuario. Ej.: user.username'
    oauth2_json_name_path: "Ruta en el JSON de usuario de OAuth2 para obtener el nombre completo del usuario: Ej.: user.name.full"
    oauth2_json_email_path: "Ruta en el JSON de usuario de OAuth2 para obtener el email. Ej.: user.email.primary"
    oauth2_email_verified: "Marca esta opción si el sitio OAuth2 verifica los correos"
    oauth2_send_auth_header: "Enviar el token como HTTP Authorization header"
    oauth2_debug_auth: "Incluir información detallada de debug en los logs"
    oauth2_authorize_options: "Al autorizar, solicitar estas opciones"
